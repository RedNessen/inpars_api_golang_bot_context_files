# InPars Telegram Bot

Telegram бот для мониторинга новых объявлений об аренде недвижимости через API сервиса [InPars.ru](https://inpars.ru).

## Возможности

- Автоматический мониторинг новых объявлений о недвижимости
- Отправка уведомлений в Telegram с подробной информацией об объекте
- Гибкая фильтрация по регионам, городам, цене и другим параметрам
- Поддержка тестового режима API InPars для разработки
- Красивое форматирование сообщений с эмодзи
- Поддержка Docker и docker-compose

## Требования

- Go 1.21 или выше
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- InPars API Token (получить на [inpars.ru/profile](https://inpars.ru/profile))

## Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/RedNessen/inpars-telegram-bot.git
cd inpars-telegram-bot
```

### 2. Создание Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и скопируйте полученный токен

### 3. Настройка конфигурации

Скопируйте файл `.env.example` в `.env`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и укажите ваш Telegram Bot Token:

```bash
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
```

Для тестирования можно использовать токен по умолчанию:

```bash
INPARS_API_TOKEN=aEcS9UfAagInparSiv23aoa_vPzxqWvm
```

### 4. Установка зависимостей

```bash
make deps
```

### 5. Запуск бота

#### Вариант 1: Запуск через Go

```bash
make run
```

#### Вариант 2: Сборка и запуск бинарника

```bash
make build
./bin/inpars-telegram-bot
```

#### Вариант 3: Запуск через Docker Compose (локальная сборка)

```bash
docker-compose up -d
```

Просмотр логов:

```bash
docker-compose logs -f
```

#### Вариант 4: Использование готового образа из GHCR (для продакшена)

Образ автоматически публикуется в GitHub Container Registry.

```bash
# Скачать образ
docker pull ghcr.io/rednessen/inpars_api_golang_bot_context_files:latest

# Запустить с переменными окружения
docker run -d \
  --name inpars-bot \
  -e TELEGRAM_BOT_TOKEN=your_token \
  -e DEFAULT_REGIONS=39 \
  -e MIN_COST=25000 \
  -e MAX_COST=50000 \
  -e FLOOR_MIN=3 \
  ghcr.io/rednessen/inpars_api_golang_bot_context_files:latest
```

**Для использования в отдельном репозитории инфраструктуры** см. [DOCKER.md](DOCKER.md)

## Использование

### Команды бота

После запуска откройте вашего бота в Telegram и используйте следующие команды:

- `/start` - Начать получать уведомления о новых объявлениях
- `/stop` - Остановить уведомления
- `/help` - Показать список доступных команд

### Пример сообщения от бота

```
🏠 2-к квартира, 55 м², 9/14 эт.

📌 Сдам • Собственник

💰 43 000 ₽ / месяц

📍 Москва, Открытое ш., 24к11
🚇 Бульвар Рокоссовского

Характеристики:
🛏 Комнат: 2
📐 Площадь: 55.0 м²
🏢 Этаж: 9/14
🧱 Материал: панельный

Условия аренды:
💳 Залог: 30 000 ₽

📝 Сдаю 2 комнатную квартиру 55 кв м., 9 этаж (14 эт.) панельного дома...

👤 Контакт: Оксана
📞 Телефон: +79000000000

🔗 Посмотреть объявление

📌 Источник: avito.ru
```

## Конфигурация

### Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота (обязательно) | - |
| `INPARS_API_TOKEN` | Токен InPars API | Тестовый токен |
| `POLL_INTERVAL` | Интервал проверки новых объявлений (сек) | 60 |
| `MAX_LISTINGS` | Макс. количество объявлений за запрос | 50 |
| `DEFAULT_REGIONS` | ID регионов для мониторинга (через запятую) | 77 (Москва) |
| `DEFAULT_CITIES` | ID городов для мониторинга (через запятую) | - |
| `TYPE_AD` | Типы объявлений: 1-сдам, 2-продам, 3-сниму, 4-куплю | 1 |
| `SELLER_TYPES` | Типы продавцов: 1-собственник, 2-агент, 3-застройщик | 1,2,3 |
| `MIN_COST` | Минимальная цена | 0 |
| `MAX_COST` | Максимальная цена | 0 |
| `FLOOR_MIN` | Минимальный этаж | 0 |
| `FLOOR_MAX` | Максимальный этаж | 0 |

### Примеры фильтров

#### Мониторинг аренды в Москве и Санкт-Петербурге до 50 000 ₽

```bash
DEFAULT_REGIONS=77,78
TYPE_AD=1
MAX_COST=50000
```

#### Только объявления от собственников

```bash
SELLER_TYPES=1
```

#### Покупка недвижимости

```bash
TYPE_AD=2
MIN_COST=3000000
MAX_COST=10000000
```

#### Аренда в Калининграде от 25 000 до 50 000 ₽, этаж от 3

```bash
DEFAULT_REGIONS=39
TYPE_AD=1
MIN_COST=25000
MAX_COST=50000
FLOOR_MIN=3
```

## Структура проекта

```
.
├── cmd/
│   └── bot/
│       └── main.go           # Точка входа приложения
├── internal/
│   ├── config/
│   │   └── config.go         # Конфигурация
│   ├── inpars/
│   │   ├── client.go         # HTTP клиент для InPars API
│   │   └── types.go          # Типы данных API
│   ├── monitor/
│   │   └── monitor.go        # Сервис мониторинга
│   └── telegram/
│       └── bot.go            # Telegram бот
├── .env.example              # Пример конфигурации
├── .gitignore
├── Dockerfile                # Docker образ
├── docker-compose.yml        # Docker Compose конфигурация
├── go.mod
├── go.sum
├── Makefile                  # Команды для сборки и запуска
└── README.md                 # Документация
```

## API InPars

### Тестовый режим

По умолчанию используется тестовый токен `aEcS9UfAagInparSiv23aoa_vPzxqWvm` с ограничениями:

- 1 запрос в секунду
- Максимум 50 объявлений за запрос
- Показывает тестовые данные (некоторые поля скрыты)

### Продакшн режим

Для полноценной работы:

1. Зарегистрируйтесь на [inpars.ru](https://inpars.ru)
2. Оплатите тариф с поддержкой API
3. Скопируйте токен на странице [профиля](https://inpars.ru/profile)
4. Укажите токен в `.env`:

```bash
INPARS_API_TOKEN=ваш_реальный_токен
```

Лимиты для платного аккаунта:

- 10 запросов в минуту
- До 1000 объявлений за запрос
- Доступ ко всем полям данных

### Справочники регионов и городов

Для получения ID регионов и городов используйте API:

```bash
# Список регионов
curl -H "Accept: application/json" \
  "https://inpars.ru/api/v2/region?access-token=aEcS9UfAagInparSiv23aoa_vPzxqWvm"

# Список городов региона (например, Москва - ID 77)
curl -H "Accept: application/json" \
  "https://inpars.ru/api/v2/city?regionId=77&access-token=aEcS9UfAagInparSiv23aoa_vPzxqWvm"
```

## Разработка

### Доступные команды Make

```bash
make help          # Показать все доступные команды
make deps          # Установить зависимости
make build         # Собрать приложение
make run           # Запустить приложение
make clean         # Удалить собранные файлы
make test          # Запустить тесты
make fmt           # Форматировать код
make docker-build  # Собрать Docker образ
```

### Запуск тестов

```bash
make test
```

### Форматирование кода

```bash
make fmt
```

## Решение проблем

### Ошибка: rate limit exceeded

Вы превысили лимит запросов. Для тестового токена разрешен 1 запрос в секунду. Увеличьте `POLL_INTERVAL`:

```bash
POLL_INTERVAL=120  # 2 минуты
```

### Бот не отправляет сообщения

1. Убедитесь, что вы отправили команду `/start` боту
2. Проверьте логи приложения
3. Убедитесь, что `TELEGRAM_BOT_TOKEN` указан правильно

### Ошибка аутентификации API

Проверьте правильность `INPARS_API_TOKEN` в файле `.env`.

## Лицензия

MIT License

## Автор

[RedNessen](https://github.com/RedNessen)

## Ссылки

- [InPars API Документация](https://inpars.ru/api)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Go Telegram Bot API](https://github.com/go-telegram-bot-api/telegram-bot-api)
